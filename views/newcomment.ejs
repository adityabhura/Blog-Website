<%- include("partials/header") %>

<div class="ui main text container segment">
    <div class="ui huge header">New Comment for <%= blog.title %></div>
    <form action="/blogs/<%= blog._id %>/comment"  method="POST" class="ui form">
        <div class="field">
            <label>Comment</label>
            <input type="text" name="comment[title]" placeholder="Text">
        </div>
        <button class="ui blue basic button">Submit</button>
    </form>
</div>


<%- include("partials/footer") %>